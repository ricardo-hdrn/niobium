name: niobium_flutter
description: "Niobium widget library — forms, display, theme for MCP-driven UIs"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  window_manager: ^0.5.1
  file_selector: ^1.0.3
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
